import UserInfo from "@/components/UserInfo"; // Importing the UserInfo component from the components directory

const page = () => { // Declaring a functional component named page
  return (
    <UserInfo /> // Rendering the UserInfo component
  );
};

export default page; // Exporting the page component as the default export
